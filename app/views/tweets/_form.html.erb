<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= form.number_field :user_id,:value => current_user.id,readonly:true,hidden:true%>
    <%= form.text_field :user_name,:value => current_user.username,readonly:true,hidden:true%>
    <%= form.text_field :tweet_time,:value => DateTime.now ,readonly:true,hidden:true %>


  <div class="field">
    <p><%= form.label :content %></p>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
