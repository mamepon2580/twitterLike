<%= render 'head', tweet: @tweet %>

<table class="table table-sm table-bordered">
  <tbody>
    <% @relations_follower.each do |relation| %>
      <tr>
        <td><%= relation.follow_name %></td>
        <td><%= link_to 'Destroy', relation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
